export interface AddNote {
    name: string;
    description: string;
    user_id: string;
    category_id: string
}

export interface UpdateNote {
    name: string;
    description: string;
    user_id: string;
    category_id: string
}